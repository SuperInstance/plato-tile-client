[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-tile-client"
version = "0.1.0"
description = "HTTP client for PLATO tile server — search, score, dedup"
license = {text = "MIT"}
requires-python = ">=3.8"
